name: sqlite_wrapper_server
description: Dart-based gRPC server for remote SQLite database access with user authentication.
version: 1.2.6
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=3.6.1 <4.0.0"

# Add regular dependencies here.
dependencies:
  sqlite_wrapper: ^0.5.3
  grpc: ^5.1.0
  protobuf: ^6.0.0
  crypto: ^3.0.0
  dart_jsonwebtoken: ^3.4.1
  uuid: ^4.5.1
  inject_x: ^0.0.6
  args: ^2.6.0
  cryptography_plus: ^3.0.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
